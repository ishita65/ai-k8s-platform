apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: sample-app-pdb
spec:
  minAvailable: 0  # intentional violation: must be at least 1
  selector:
    matchLabels:
      app: sample-app
